import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.brownhill.app',
  appName: '\x16\x16Brownhill Partnership',
  webDir: 'dist'
};

export default config;
